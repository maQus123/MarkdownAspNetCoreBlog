@model MarkdownAspNetCoreBlog.Models.Post

@{
    ViewData["Title"] = Model.Title;
}

<h2>@Model.Title</h2>
<p>@Model.CreatedAt.ToString("d. MMMM yyyy") von <a href="#">maQus</a></p>
<md>@Model.Content</md>