@model MarkdownAspNetCoreBlog.Models.Post

@{
    ViewData["Title"] = "Post löschen: " + Model.Title;
}

<div class="container">
    <form method="post" asp-action="DeleteConfirmed" asp-controller="Posts">
        <p>Willst den den Post '@Model.Title' wirklich löschen?</p>
        <button type="submit" class="btn btn-danger">Löschen</button>
        <a asp-route="list-posts" role="button" class="btn btn-default">Abbrechen</a>
    </form>
</div>