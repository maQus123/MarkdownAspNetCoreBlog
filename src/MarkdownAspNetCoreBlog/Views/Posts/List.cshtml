@model IEnumerable<MarkdownAspNetCoreBlog.Models.Post>

@{
    ViewData["Title"] = "Posts verwalten";
}


<table>
    <tr>
        <th>Nr.</th>
        <th>Title</th>
        <th>Datum</th>
        <th>Veröffentlicht</th>
        <td></td>
    </tr>
    @{int i = 1;}
    @foreach (var post in Model) {
        <tr>
            <td>@i</td>
            <td>@post.Title</td>
            <td>@post.CreatedAt.ToString("d MM yyyy hh:mm")</td>
            <td>@post.IsPublished.ToString()</td>
            <td><a asp-route="update-post" asp-route-id="@post.Id">Bearbeiten</a></td>
        </tr>
        i++;
    }
</table>
<a class="btn btn-primary" asp-route="create-post" role="button">Neuen Post</a>