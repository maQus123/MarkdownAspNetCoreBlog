@model MarkdownAspNetCoreBlog.Models.Image

@{
    ViewData["Title"] = "Bild löschen: " + Model.Name;
}

<div class="container">
    <form method="post" asp-action="DeleteConfirmed" asp-controller="Images">
        <p>Bild '@Model.Name' wirklich löschen?</p>
        <button type="submit" class="btn btn-danger">Löschen</button>
        <a asp-route="list-images" role="button" class="btn btn-default">Abbrechen</a>
    </form>
</div>